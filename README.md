# Loyiha

Talablar bo‘yicha tayyorlangan demo:

- React + TypeScript + Vite
- ESLint + Prettier (`npm run lint`, `npm run format`)
- UI kit: Material UI (MUI)
- State management: Zustand (`src/store/usersStore.ts`)
- Forms & validation: `react-hook-form` + `zod` + `@hookform/resolvers`
- Storage: `localStorage` (users va polygonlar sahifa yangilansa ham saqlanadi)
- Maps: Leaflet + React-Leaflet, polygon chizish va saqlash
- Responsive layout: mobil va desktop uchun mos

### Talab qilinadigan versiyalar

- **Node.js**: 18.x yoki 20.x (LTS, masalan `v18.18.0`)
- **npm**: 9.x yoki 10.x

### Buyruqlar

```
npm install
npm run dev
npm run build
```

Keyin brauzerda `http://localhost:5173` manzilini oching.
# agriculture
